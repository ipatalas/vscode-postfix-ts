# Coverage

Coverage is tracked with c8 and configured in .c8rc.json.

Run coverage:
- npm run test-with-coverage

Reports:
- Terminal summary
- HTML report (if configured)
- Only includes src/ (excludes tests and build files)
