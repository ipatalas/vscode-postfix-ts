# Publishing

- Always run the production build before packaging
- Package: npm run package (creates .vsix)
- Publish: npm run deploy (requires publisher credentials)
- Versioning follows semver in package.json
