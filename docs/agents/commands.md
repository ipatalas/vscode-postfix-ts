# Commands

## Install
- npm ci

## Build
- npm run compile — development build (sourcemaps)
- npm run vscode:prepublish — production build (minified, no sourcemaps)
- node build.mjs --watch — watch mode

## Lint
- npm run lint

## Test
- npm test — see docs/agents/testing.md for details

## Package
- npm run package
